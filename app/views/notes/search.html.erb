<%= link_to "Home", root_path %>
<br/>

<%= form_tag search_path, method: :get do %>
  <%= text_field_tag :query %>
<% end %>

<ul class="root-container">
  <% @notes.each do |note| %>
    <li><%= render "show", note: note, root: true %></li>
  <% end %>
</ul>
